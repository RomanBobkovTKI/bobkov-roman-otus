<div class="row">
        {{#if message}}
            <div class="alert {{messageClass}}" role="alert">
                {{message}}
            </div>
        {{/if}}
    
        <h2 class="center">На нашей платформе вы можете создать свой курс и научиться чему всегда мечтали.</h2>
        <div class="col s12 m6">
            <a class="center waves-effect waves-light btn btn-primary" 
                href="/protected/addCourse">
                Создать курс
            </a>
        </div>
</div>

    <h4 class="center">Курсы</h4>

    {{#if allCourses.length}}
    {{#each allCourses}}
        <div class="row">
            <div class="col s12 m6">
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                <span class="card-title">{{title}}</span>
                <p>{{desc}}</p>
                </div>
                <div class="card-action">
                <a href="/protected/courses/{{_id}}">Открыть</a>
                <a href="/protected/courses/{{_id}}/edit?allow=true">Редактировать</a>
                <a href="/protected/courses/{{_id}}/delete" method="delete">Удалить</a>
                </div>
            </div>
            </div>
        </div>
    {{/each}}
    {{else}}
        <p class="center">Курсов пока нет</p>
    {{/if}}
</div>